"""Abstract RequestContext base class."""

from abc import ABC


class BaseRequestContext(ABC):
    """
    Abstract RequestContext base class.

    This class is only for resolving recursive import issues.
    """
